using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "ScriptPorte", menuName = "Script Obstacle/Porte")]
public class ScriptablePorte : ScriptableObstacle
{
    public ScriptableLevier scriptLevier;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
